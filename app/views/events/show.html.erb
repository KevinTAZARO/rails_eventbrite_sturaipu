<%= render @event %>

<div>
  <%= link_to "Edit this event", edit_event_path(@event) %> |
  <%= link_to "Back to events", events_path %><br/><br/>

  <%= button_to "Destroy this event", @event, method: :delete %><br/>

  <%= button_to "Rejoindre l'event", new_event_attendance_path(@event.id), method: :get %>

  Nombre de participant(s) : <%= @event.users.count %>
</div>
